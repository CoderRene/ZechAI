# React (Vite) — exposed to the browser; must be prefixed with VITE_
# Use either the backend base (HTTPS) or the full WebSocket URL:
VITE_BACKEND_BASE_URL=https://your-url.com
VITE_WS_SPEC_URL=wss://your-url.com/ws/generate-spec

